1. To install material ui libraries - npm install @mui/material @emotion/react @emotion/styled @mui/x-data-grid @mui/icons-material react-router-dom@6 react-pro-sidebar formik yup @fullcalendar/core @fullcalendar/daygrid @fullcalendar/timegrid @fullcalendar/list @nivo/core @nivo/pie @nivo/line @nivo/bar @nivo/geo

--Add import {BrowserRouter} from "react-router-dom" to main.jsx file
--Make the changes in index.css file by defining the styling you want for your project

2. Make the data folder to store the data in it. Two files are created in data folder to store relevant data.
3. Design the arhitecture and folders for your project according to UI.
4. -- create a arrow function in each jsx file like  -  
            const Sidebar = () => {
                return(
                    <div>
                        Sidebar
                    </div>
                );
            }
            export default Sidebar;
            
5. Create theme.js file for storing all the colors 
    1. use Tailwind shades extension to get the multiple shades of a color.
    2. Write a color, select the color and press "ctrl+k ctrl+g" to get the multiple shades.
    --- This file basically contains all the colors that will be used for light and dark mode. It also has the function that will change the colors according to dark or light mode. So, by exporting the function, we can have the light and dark mode functionality.